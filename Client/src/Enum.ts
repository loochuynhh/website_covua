export enum Color{
    WHITE,
    BLACK,
    NOT
} 
export enum GameStatus {
    WIN,
    LOSE,
    DRAW,
    DRAW_REQUEST,
    DRAW_DENY,
    DRAW_ACCEPT,
    QUIT,
    SURRENDER,
    ACTIVE
} 
export enum Catsling{
    NOT,
    NEAR,
    FAR,
    DONE,
    EXCUTE
}
export enum Invite{
    Request,
    Accept,
    Deny
}